import SwiftUI

// Developer-only catalog view; stubbed while component APIs are in flux.
public struct ComponentCatalogView: View {
    public init() {}
    public var body: some View {
        Text("Component Catalog")
    }
}
