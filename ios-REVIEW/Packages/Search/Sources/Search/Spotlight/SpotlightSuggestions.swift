import Foundation

// SpotlightSuggestions stub — disabled while CoreSpotlight CSSearchQuery /
// CSUserQueryContext / CSSearchableItemInteraction APIs are not available
// in the current SDK. Re-enable once the project bumps to a SDK version
// where these public APIs land.

public enum SpotlightSuggestions {
    public static func donate(_ noop: String = "") {}
}
